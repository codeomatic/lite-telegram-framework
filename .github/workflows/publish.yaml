name: Publish to PyPI
on:
  push:
    branches:
      - deploy

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build package
      - run: |
          python3 setup.py sdist bdist_wheel


